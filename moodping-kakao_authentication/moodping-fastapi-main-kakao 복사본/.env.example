# ============================================================
# MoodPing FastAPI 환경변수 템플릿
# 이 파일을 복사해서 .env 로 만들고 실제 값을 채워넣으세요.
# cp .env.example .env
# ============================================================

# LLM 제공자 선택: openai | gemini | claude
LLM_PROVIDER=openai

# 각 LLM 모델명 (변경하지 않아도 됩니다 - 기본값이 자동 적용됩니다)
OPENAI_MODEL=gpt-4.1-mini
GEMINI_MODEL=gemini-2.5-flash
CLAUDE_MODEL=claude-haiku-4-5-20251001

# OpenAI API 키 (LLM_PROVIDER=openai 시 필요)
OPENAI_API_KEY=your-openai-api-key-here

# Google Gemini API 키 (LLM_PROVIDER=gemini 시 필요)
# https://aistudio.google.com/app/apikey 에서 무료 발급 가능
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic Claude API 키 (LLM_PROVIDER=claude 시 필요)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# LLM 공통 설정
LLM_TIMEOUT_SECONDS=5
LLM_MAX_TOKENS=600
LLM_TEMPERATURE=0.7

# 카카오 OAuth (https://developers.kakao.com 에서 REST API 키 발급)
# Redirect URI는 카카오 개발자 콘솔에도 동일하게 등록. 포트는 실제 서버 포트에 맞게 변경.
KAKAO_CLIENT_ID=your-kakao-rest-api-key-here
KAKAO_CLIENT_SECRET=your-kakao-client-secret-here
KAKAO_REDIRECT_URI=http://localhost:8000/kakao-auth/callback

# JWT
JWT_SECRET_KEY=change-me-in-production

# DB 설정 (Docker Compose 기본값과 일치)
DB_USER=moodping
DB_PASSWORD=moodping123
DB_HOST=localhost
DB_PORT=3307
DB_NAME=moodping
